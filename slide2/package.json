{
  "name": "exp-slidev-monorepo-slide2",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export",
    "deploy": "wrangler pages deploy ./dist --project-name $npm_package_name"
  }
}
